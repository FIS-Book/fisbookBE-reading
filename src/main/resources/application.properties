server.port=3000
spring.application.name=reading-service
spring.mvc.dispatch-options-request: true
spring.data.sendgrid.key=${SENDGRID_API_KEY}
spring.data.mongodb.uri=${MONGODB_URI_READINGS}
api.version=${API_VERSION}

# Ruta para Swagger UI
springdoc.swagger-ui.path=${api.version}/swagger-ui.html
# Ruta para la documentación de OpenAPI
springdoc.api-docs.path=${api.version}/api-docs

base.url=${BASE_URL}
jwt.secret=${JWT_SECRET}